import 'package:flutter/material.dart';

abstract class AppColors {
  AppColors._();

  // Login Page
  static const loginPrimary = Colors.white;
  static const loginSecondary = Color(0xFF8E8E8E);
  static const loginShadow = Color(0x44000000);
  static const alternateOption = Color(0xFF007699);

  // Approved View
  static const approvedGradientTop = Color(0xFF88CBDF);
  static const approvedGradientBottom = Color(0xFF31768A);

  // Typography
  static const fontBlack = Color(0xBB000000);
  static const fontGrey = Color(0x99000000);
}