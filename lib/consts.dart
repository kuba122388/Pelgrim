import 'package:flutter/material.dart';

// LOGIN PAGE
const double LOGIN_PAGE_MOVE = 0.1;
const double LOGIN_CONTAINER_SIZE = 0.7;
const double BACKGROUND_WAVES_IMAGE = 1.05;

const Color LOGIN_BG_PRIMARY_COLOR = Colors.white;
const Color LOGIN_BG_SECONDARY_COLOR = Color(0xFF8E8E8E);
const Color LOGIN_SHADOW_TEXT = Color(0x44000000);
const Color LOGIN_ALTERNATE_OPTION = Color(0xFF007699);

const Color REGISTER_LOGIN_TEXT = Color(0xBB000000);

const Offset LOGIN_SHADOW_OFFSET = Offset(2, 4);

const Color APPROVED_GRADIENT_TOP = Color(0xFFD9D9D9);
const Color APPROVED_GRADIENT_BOTTOM = Color(0xFF31768A);

//USER PAGE
const Color FONT_BLACK_COLOR = Color(0xBB000000);
const Color FONT_GREY_COLOR = Color(0x99000000);
const Color BACKGROUND_CONTAINERS_COLOR = Color.fromRGBO(217, 217, 217, 1);

const Shadow APP_TEXT_SHADOW =
    Shadow(color: Color(0x20000000), offset: Offset(2, 4), blurRadius: 6);

const double FONT_SIZE_BIG = 18;
const double FONT_SIZE_MEDIUM = 16;
const double FONT_SIZE_SMALL = 14;

//BURGER MENU
const Color LIST_TILE_INACTIVE_COLOR = Color(0x60000000);

//BOX SHADOW - SONGS
const BoxShadow BOX_SHADOW_CONTAINER = BoxShadow(
    color: Color(0x40000000),
    blurRadius: 4,
    spreadRadius: 2,
    offset: Offset(0, 0));

const contactInfo =
    "Tu powinny znaleźć się kontakty do osób pełniących określone funkcje. Przykład: \n\n"
    "KIEROWNIK PIELGRZYMKI\n"
    "Ks. Bartłomiej Zawadzki\n"
    "Tel: 123-456-789\n\n"
    "DROGÓWKA\nJAN CISZEK\nTel: 987-654-321\n"
    "itd."
;
